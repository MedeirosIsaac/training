//Questao 2 - Faça um programa que leia um valor T e preencha um vetor N[10] com a sequência de valores de 0 até T-1 repetidas vezes, conforme exemplo abaixo. Imprima o vetor N.
Entrada
A entrada contém um valor inteiro T (2 ≤ T ≤ 50).

Saída
Para cada posição do vetor, escreva "N[i] = x", onde i é a posição do vetor e x é o valor armazenado naquela posição.
//

#include <iostream>
using namespace std;

int main() {
    int T, N[10];

    cin >> T;

    if(T >= 2 && T <= 50){

    for(int i = 0; i < 10; i++) {
        N[i] = i % T;
    }
    for(int i = 0; i < 10; i++){
        cout << "N " << "[" << i << "]" << " = " << N[i] << endl;
    }
    }



    return 0;
}
